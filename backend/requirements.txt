fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
psycopg[binary]>=3.1,<4.0
pgvector>=0.3,<1.0
PyJWT>=2.8,<3.0
passlib[bcrypt]>=1.7,<2.0
bcrypt>=4.0,<5.0
python-multipart>=0.0.9,<1.0
boto3>=1.34,<2.0
email-validator>=2.1,<3.0
# LLM layer (provider-agnostic). Only imported inside app/llm/.
litellm>=1.44,<2.0
# Lightweight parsing for real-mode extraction (optional at runtime).
pypdf>=4.2,<6.0
pillow>=10.3,<12.0
# Renders PDF pages to images for multimodal extraction of scans/handwriting.
pymupdf>=1.24,<2.0
# Optional (real mode only; stub voice needs nothing): on-device speech-to-text
# for /voice/transcribe. Keeps audio on-prem (CPU int8). Uncomment to enable.
# faster-whisper>=1.0,<2.0

# Test runner.
pytest>=8.2,<9.0
